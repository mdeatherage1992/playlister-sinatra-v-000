<% if flash.has?(:message) %>
  <%= flash[:message] %>
<% end %>

<h1> <%= @song.name %> </h1>
<a href="/artists/<%= binding.pry %>"> <%= @song.artist.name %> </a>
<% @song.genres.each do |genre| %>
  <a href="/genres/<%= genre.slug %>"> <%= genre.name %> </a>
  <% end %>
